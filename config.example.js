/* Copy this file to  config.local.js  and paste your own key.
   config.local.js is git-ignored, so your key never gets committed.

   Get a free key:  https://aistudio.google.com/apikey
   (Gemini Flash has a free tier — personal use is typically $0.)

   SECURITY: only ever run this app LOCALLY (http://127.0.0.1...).
   Never deploy the page publicly with a real key in it — a client-side
   key is visible to anyone who opens the site. */

window.GEMINI_API_KEY = "";   // e.g. "AIzaSy..."
